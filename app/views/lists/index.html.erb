<h1>------list of movies-----</h1>
<% @lists.each do |list| %>
  <%= link_to list_path(list), class: 'text-decoration-none' do %>
<p><%= list.name %></p>
  <% end %>
<% end %>

<%= link_to "add a new list", new_list_path, class: 'btn btn-flat' %>

